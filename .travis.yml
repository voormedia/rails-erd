language: ruby
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.5
  - 2.2.0
  - jruby
before_install:
  - gem install bundler
  - sudo apt-get update -qq
  - sudo apt-get install -qq graphviz
script: bundle exec rake
matrix:
  allow_failures:
    - rvm: jruby
  fast_finish: true
cache: bundler
